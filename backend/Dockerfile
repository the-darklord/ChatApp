FROM node:20

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

ENV PORT=5000
ENV NODE_ENV=development
ENV MONGODB_URI=$MONGODB_URI
ENV JWT_SECRET=$JWT_SECRET
ENV ORIGIN=$ORIGIN

EXPOSE 5000

CMD ["npm", "run", "dev"]
